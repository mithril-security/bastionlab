use tch::*;

pub mod file_system {
    use crate::TrainableCModule;
    use std::collections::HashMap;
    use uuid::Uuid;

    pub struct UserMode {}
    pub struct Mode {}

    // pub fn unstream_artifacts<T>(stream: Vec<u8>, impl Fn(T)-)
}
